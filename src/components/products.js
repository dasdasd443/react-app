import {Route, BrowserRouter as Router} from 'react-router-dom';
const Products = () => {
    return (
        <Router>
            <h1>Hello world</h1>
        </Router>
    );
}

export default Products;